[project]
name = "torchmine"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Shutaro Fukuzawa", email = "fukuzawa.s.dfd1@m.isct.ac.jp" }
]
requires-python = ">=3.12"
dependencies = [
    "matplotlib>=3.11.2",
    "numpy>=2.5.3",
    "pyyaml>=6.0.3",
    "torch>=2.14.0",
]

[dependency-groups]
mnist = ["torchvision"]

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"
